
<% @categories.each do |category| %>

    <h2><%= category.name %></h2>
    <%= link_to "Ver Categoria", category_path(category), class: "btn btn-default" %>

<% end %>
<%= link_to "Criar Categoria", new_category_path, class: "btn btn-primary" %>