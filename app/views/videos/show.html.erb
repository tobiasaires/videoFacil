<%= "Categoria do video #{@category}" %>
<%= "Nome do video #{@video.title}"%>

<%= render @video.comments %>

<iframe width="420" height="315"
src= "<%= url_to_id @video.url %>" >
</iframe>

<%= render 'comments/form'%>